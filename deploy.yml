name: Deploy to Vercel + Purge Cloudflare Cache

on:
  push:
    branches:
      - main        # change to master if that is your default branch

jobs:
  deploy:
    name: Deploy & Purge Cache
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repo
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Install Vercel CLI
      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      # 3. Pull Vercel environment / project settings
      - name: Pull Vercel environment info
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # 4. Build the project
      - name: Build project
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # 5. Deploy the build output to Vercel production
      - name: Deploy to Vercel production
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "deployment_url=$url" >> $GITHUB_OUTPUT
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # 6. Purge Cloudflare cache — runs only after a successful deploy
      #    Purges everything so the new /assets/* hashed filenames and
      #    fresh index.html are served immediately from Cloudflare's edge.
      - name: Purge Cloudflare cache
        if: steps.deploy.outcome == 'success'
        run: |
          echo "Purging Cloudflare cache for zone ${{ secrets.CLOUDFLARE_ZONE_ID }}..."
          response=$(curl -s -o response.json -w "%{http_code}" -X POST \
            "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}')
          cat response.json
          if [ "$response" != "200" ]; then
            echo "Cloudflare purge failed with HTTP $response"
            exit 1
          fi
          echo "Cloudflare cache purged successfully."

      # 7. Print deployment summary
      - name: Summary
        if: always()
        run: |
          echo "### Deploy Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Vercel URL:** ${{ steps.deploy.outputs.deployment_url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Cloudflare purge:** ${{ steps.deploy.outcome == 'success' && '✅ Done' || '⚠️ Skipped / failed' }}" >> $GITHUB_STEP_SUMMARY
